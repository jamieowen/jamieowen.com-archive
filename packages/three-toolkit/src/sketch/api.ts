// type CreateSketch = params;

type SketchParams = {
  container?: undefined | string | HTMLElement;
};

type SketchHooks = {
  render: () => {};
};

// sketch(({scene,camera}}=>{

// // declare vars

//   return (delta,mouseDown,etc)=>{
//
//   }

// or

//   return (delta,mouseDown,etc)=>{
//
//   }

// });

// Renderer
