import { CSS_NAMES, IColor } from "@thi.ng/color";

const generateTwoColor = () => {
  // take a color.. ( or generate random )
  // round to nearest something... degree of hue or ratio.
  // step around the wheel by some degree N times.
  // for each color, generate a shade and a highlight.
  // aim for a specfic contrast ratio?
  // or a balanced total luminance across all colors?
};

// const permuteColorRotate = ( color:IColor )

// take a color.
// create a complement rotation... with an optional min max offset.

// Luminance balance.
// Normalise/Limit 2 colours ( or number of colours? ) to an overall sum luminance

// Can the same be done to reduce or increase a contrast ratio?

// distribution. create a numner of additional colours as tints/adjustments of provided base colours.
// supply a 'percentage split' i.e. 60, 30,10 rule.
