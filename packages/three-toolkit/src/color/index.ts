import { CSS_NAMES } from "@thi.ng/color";

const generateTwoColor = () => {
  // take a color.. ( or generate random )
  // round to nearest something... degree of hue or ratio.
  // step around the wheel by some degree N times.
  // for each color, generate a shade and a highlight.
  // aim for a specfic contrast ratio?
  // or a balanced total luminance across all colors?
};
