// import React, { FC } from "react";
// import { render, screen } from "@testing-library/react";
// import { renderHook } from "@testing-library/react-hooks";
// import { IntersectionProvider, useMotionContext } from "../__observer-context";

// const TestComp: FC<{ title: any }> = ({ title }) => {
//   return <h1>{title}</h1>;
// };

test("", () => {
  console.log("okok");

  // render(<TestComp title="Hello World!" />);
  // expect(screen.getByRole("heading")).toHaveTextContent("Hello World!");
  // const { result } = renderHook(() => useMotionContext(), {
  //   wrapper: ({ children }) => {
  //     return <IntersectionProvider>{children}</IntersectionProvider>;
  //   },
  // });

  // console.log(result);
});
