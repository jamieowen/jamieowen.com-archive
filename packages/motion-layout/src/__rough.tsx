import { StateMachine, Machine } from "xstate";

const Stateful = () => {
  // Create a component class and initialise its state.
  return <Fragment></Fragment>;
};

interface ISceheduledComponent {
  fsm: Machine;
}

class StateComponent {
  xstate;
}

interface IScheduledComponent {
  fsm: StateMachine;
}
