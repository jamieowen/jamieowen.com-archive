import { DeferredNodeDecl,breadthFirstIterator } from './decl';

const graph = root([
  group(i=>[]),
  group(i=>[]),
  group(i=>[
    group(i=>[]),
    group(i=>[
      group(i=>[]),
    ]),
  ]),
  group(i=>[])
])


class XLayout{

  constructor(){

  }

}