const PI = Math.PI;
const TAU = PI * 2;
const INV_TAU = 1 / TAU;
const DEG2RAD = PI / 180;
const RAD2DEG = 180 / PI;
let EPS = 1e-6;

export { DEG2RAD as D, EPS as E, INV_TAU as I, RAD2DEG as R, TAU as T };
